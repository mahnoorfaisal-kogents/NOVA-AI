/**
 * NOVA views import the browser client from here. The actual client is the
 * generated integration client, so session handling stays consistent.
 */
export { supabase } from "@/integrations/supabase/client";
